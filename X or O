//Create a function that takes a string, checks if it has the same number of 'x's and 'o's and returns either true or false

function XO(str) {
  const arr = str.split('')
  return (
    arr.filter(char => char.toLowerCase() === 'x').length
		===
    arr.filter(char => char.toLowerCase() === 'o').length
  )
}
